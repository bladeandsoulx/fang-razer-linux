{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs (events, window) used by the UI bridge",
  "windows": ["main"],
  "permissions": ["core:default"]
}
